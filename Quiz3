/***********************************************************
Author: Zijun Cen
Date: 1/30/2018
Effort: 20 minutes
Purpose: learn the left shift operator
***********************************************************/
#include <stdio.h>

int main(int argc, char* argv[])
{
	unsigned int x = 1;
	int j;
	while (x != 0)
	{
		for (j = 0; j < 32; j++)
		{	
			printf("%d: %u\n", j, x);
			x = x << 1;
		}
	}
	getchar();
	return 0;
}
